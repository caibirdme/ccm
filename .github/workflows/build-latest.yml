name: build-latest

on:
  push:
    branches: [ main ]

permissions:
  contents: read

jobs:
  build:
    name: Build latest (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest, macos-14 ]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust (actions-rs)
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: 1.90.0
          override: true

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Build
        run: cargo build --locked --release

      - name: Package
        shell: bash
        run: |
          mkdir -p dist
          cp target/release/ccm dist/
          tar -C dist -czf ccm-latest-${{ runner.os }}-${{ runner.arch }}.tar.gz ccm

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ccm-latest-${{ runner.os }}-${{ runner.arch }}
          path: ccm-latest-${{ runner.os }}-${{ runner.arch }}.tar.gz
          retention-days: 7
